#!/bin/bash

rm -r lib
rm -r node_modules

# after that fix the package.json
# run npm install
# fix the code files 
# run npm run build
# validating you have the appropriate version in the package.json
# make sure everything is committed
# npm publish
